export default function importJSON(...modules) {
    if (modules.length === 1) {
        return [modules[0].locale_id, modules[0].json];
    }
    else {
        return [modules[0].locale_id, [].concat(...modules.map(mod => mod.json))];
    }
}
